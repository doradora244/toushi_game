# ============================================================
# challenges.py — コーディングチャレンジ問題集（コード実行型）
# ============================================================
# プレイヤーは「完全なPythonコード」を自分で書いて実行します。
# 採点は「コードを実際に動かして出力を確認」する方式です。
# ============================================================

CHALLENGES = [

    # =====================================================
    # 問題1: 変数（Variable）
    # =====================================================
    {
        "id": 1,
        "title": "【変数】資金を変数に入れて表示しよう",
        "level": "入門",
        "topic": "変数",

        "description": """\
プレイヤーの初期資金は **100000 円** です。

`money` という変数に 100000 を代入して、`print()` で表示するコードを書いてください。

実行すると、こう出力されます：
```
100000
```
""",

        # テキストエリアの初期値（ここから書き始める）
        "starter_code": "money = \nprint(money)",

        # ヒント: 書き方・例・完成形を全部見せる
        "hint": """\
📌 書き方
    変数名 = 値

📌 例（別の変数で練習）
    score = 200
    print(score)
    # → 200 と表示される

📌 あなたが書くコード
    money = 100000    ← money に 100000 を入れる
    print(money)      ← 表示すると「100000」と出る""",

        # 期待される出力（実行結果と比較する）
        "expected_output": "100000",

        # "exact" = 完全一致、"contains" = 含まれていれば正解
        "validation_type": "exact",

        # 不正解のときに表示する模範解答
        "answer_example": "money = 100000\nprint(money)",

        "explanation": """\
**変数**とは、値を保存するための「名前のついた箱」です。

```python
money = 100000  # money という箱に 100000 を入れる
print(money)    # 箱の中身を表示 → 100000
```

**数値と文字列の違い（重要！）**

| 書き方 | 型 | 計算できる？ |
|--------|-----|------|
| `100000` | int（整数） | ✅ できる |
| `"100000"` | str（文字列） | ❌ できない |

```python
# ✅ 数値なので計算できる
money = 100000
profit = money * 0.1   # → 10000.0

# ❌ 文字列なのでエラーになる
money = "100000"
profit = money * 0.1   # → TypeError
```
""",

        "common_mistakes": [
            '`"100000"` と引用符をつけると文字列（str）になってしまいます',
            '`100,000` とカンマを入れると SyntaxError になります',
            '`print(Money)` のように大文字にすると NameError になります（変数名は一致させる）',
        ],

        "reward_message": "変数の基本をマスター！資金管理がうまくなりました 💰",
    },


    # =====================================================
    # 問題2: 条件分岐（if 文）
    # =====================================================
    {
        "id": 2,
        "title": "【条件分岐】バグが多いか判定しよう",
        "level": "入門",
        "topic": "条件分岐",

        "description": """\
`bug_rate`（バグ率）が **20 より大きい** とき、`警告！バグが多いです` と表示するコードを書いてください。

実行すると、こう出力されます：
```
警告！バグが多いです
```
""",

        "starter_code": "bug_rate = 25\n\n# bug_rate が 20 より大きいとき「警告！バグが多いです」と表示\n",

        "hint": """\
📌 書き方
    if 条件式:
        実行したい処理（インデント＝字下げが必要！）

📌 例（別の条件で練習）
    score = 90
    if score > 80:
        print("合格！")
    # → 合格！ と表示される

📌 あなたが書くコード
    bug_rate = 25
    if bug_rate > 20:          ← 「20 より大きい」は > を使う
        print("警告！バグが多いです")  ← 4 スペースのインデント必須！""",

        "expected_output": "警告！バグが多いです",
        "validation_type": "exact",
        "answer_example": "bug_rate = 25\nif bug_rate > 20:\n    print(\"警告！バグが多いです\")",

        "explanation": """\
**条件分岐（if 文）**は「もし〜なら○○する」を表します。

```python
bug_rate = 25

if bug_rate > 20:              # もし bug_rate が 20 より大きいなら
    print("警告！バグが多いです")  # ← インデント（4スペース）必須！
```

**⚠️ インデントとは？**
```python
if bug_rate > 20:
    print("OK")    # ✅ 4スペース字下げ → if の中の処理

print("これはifの外")  # ✅ 字下げなし → 常に実行される
```

**比較演算子の一覧**

| 記号 | 意味 | 例 |
|------|------|----|
| `>`  | より大きい | `25 > 20` → True |
| `>=` | 以上 | `20 >= 20` → True |
| `<`  | より小さい | `10 < 20` → True |
| `==` | 等しい | `20 == 20` → True |

**`=` と `==` の違い（超重要！）**
```python
x = 5      # 「=」は代入（右の値を左に入れる）
x == 5     # 「==」は比較（等しいかどうか判定する）
```
""",

        "common_mistakes": [
            'インデント（字下げ）を忘れると IndentationError になります',
            'コロン `:` を忘れると SyntaxError になります',
            '`=` と書いてしまう（代入になる。比較は `==`）',
            '`print` の中の文字列と期待出力が一字でも違うと不正解になります',
        ],

        "reward_message": "条件分岐を習得！バグ検知システムが強化されました 🐛→✅",
    },


    # =====================================================
    # 問題3: ループ（for 文）
    # =====================================================
    {
        "id": 3,
        "title": "【ループ】ユーザーに通知を送ろう",
        "level": "入門",
        "topic": "ループ",

        "description": """\
`range()` を使って、**1 から 5** を順番に表示するコードを書いてください。

実行すると、こう出力されます：
```
1
2
3
4
5
```
""",

        "starter_code": "# range(5) を使って 1, 2, 3, 4, 5 を順番に表示してください\n",

        "hint": """\
📌 書き方
    for 変数名 in range(回数):
        繰り返す処理

📌 range() の動き
    range(5) → 0, 1, 2, 3, 4 の 5 回（0 から始まる！）

📌 例（0 から表示する場合）
    for i in range(5):
        print(i)
    # → 0, 1, 2, 3, 4 と表示される

📌 あなたが書くコード（1 から表示したい → i + 1 にする）
    for i in range(5):
        print(i + 1)   ← i が 0 から始まるので +1 すると 1〜5 になる""",

        "expected_output": "1\n2\n3\n4\n5",
        "validation_type": "exact",
        "answer_example": "for i in range(5):\n    print(i + 1)",

        "explanation": """\
**ループ（for 文）**は同じ処理を繰り返すときに使います。

```python
for i in range(5):         # i = 0, 1, 2, 3, 4 と変化しながら 5 回繰り返す
    print(i + 1)           # 1, 2, 3, 4, 5 と表示される
```

**range() の動き**

| 書き方 | 生成される数 | 回数 |
|--------|------------|------|
| `range(5)` | 0, 1, 2, 3, 4 | 5 回 |
| `range(1, 6)` | 1, 2, 3, 4, 5 | 5 回（こちらでも正解！） |
| `range(0, 10, 2)` | 0, 2, 4, 6, 8 | 5 回 |

**ループが便利な理由**
```python
# ループなし（同じコードを 5 回書く）
print(1)
print(2)
print(3)
print(4)
print(5)

# ループあり（2 行で済む！）
for i in range(5):
    print(i + 1)
```
""",

        "common_mistakes": [
            '`Range` と大文字で書く（Python は大文字小文字を区別します）',
            'インデント（字下げ）を忘れると IndentationError になります',
            'コロン `:` を忘れると SyntaxError になります',
            '`range(5)` のまま print すると 0〜4 になる（1〜5 にするには i + 1）',
        ],

        "reward_message": "ループをマスター！一度に多くのユーザーへ通知できるようになりました 🔄",
    },
]


# ============================================================
# ヘルパー関数
# ============================================================

def get_challenge(index):
    """指定インデックスの問題を返す（存在しなければ None）"""
    if 0 <= index < len(CHALLENGES):
        return CHALLENGES[index]
    return None


def get_total_challenges():
    """チャレンジの総数を返す"""
    return len(CHALLENGES)
